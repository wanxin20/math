---
description: React前端代码规范 - 教师教研论文评选平台
globs:
  - "**/*.tsx"
  - "**/*.ts"
  - "**/*.jsx"
  - "**/*.js"
---

## 🎯 核心原则
- React 19函数式组件 + TypeScript严格模式
- 遵循React最佳实践和Hooks规范
- 所有代码必须通过ESLint检查后才能提交

## 📁 目录架构（严格遵守）
```
/
├── components/        # 可复用组件
├── pages/            # 页面组件（对应路由）
├── services/         # API服务层
├── types.ts          # TypeScript类型定义
├── constants.ts      # 常量配置
├── i18n.ts           # 国际化
└── App.tsx           # 应用入口
```

## 🏷️ 命名约定
- **组件文件**: PascalCase（UserProfile.tsx）
- **组件**: PascalCase（UserProfile）
- **Hooks**: camelCase + use前缀（useUserData）
- **工具函数**: camelCase（formatDate）
- **常量**: UPPER_SNAKE_CASE（API_BASE_URL）
- **接口/类型**: PascalCase（User, Competition）

## ✅ 必须遵循
1. **组件定义**: 使用React.FC<Props>或函数声明
2. **Props接口**: 所有组件必须定义Props接口
3. **类型安全**: 禁止使用any，必须明确类型
4. **解构Props**: 组件内部优先解构props
5. **Hooks顺序**: useState → useEffect → 自定义hooks
6. **条件渲染**: 使用三元或&&，避免复杂嵌套
7. **事件命名**: handle前缀（handleClick, handleSubmit）

## 🚫 严禁行为
- ❌ 禁止在组件内定义子组件
- ❌ 禁止在render中直接绑定函数（使用useCallback）
- ❌ 禁止useEffect中缺少依赖项
- ❌ 禁止直接修改state（使用不可变更新）
- ❌ 禁止在循环中使用索引作为key
- ❌ 禁止过度使用useEffect（能用派生状态就不用effect）
- ❌ 禁止组件超过300行（拆分）

## 🔍 代码生成后强制检查
**每次生成或修改代码后，必须执行以下命令并确保通过：**
```bash
npm run lint
```
如有错误，立即修复后再继续。不通过Lint检查的代码不得提交。

## 📝 代码模板
```typescript
// 标准组件模板
interface UserCardProps {
  user: User;
  onUpdate?: (id: string) => void;
}

const UserCard: React.FC<UserCardProps> = ({ user, onUpdate }) => {
  const [isEditing, setIsEditing] = useState(false);

  const handleEdit = useCallback(() => {
    setIsEditing(true);
  }, []);

  useEffect(() => {
    // 副作用逻辑
    return () => {
      // 清理逻辑
    };
  }, [user.id]);

  return (
    <div className="user-card">
      <h3>{user.name}</h3>
      <button onClick={handleEdit}>编辑</button>
    </div>
  );
};

export default UserCard;
```

## 🎨 风格偏好
- 双引号（JSX属性），单引号（JS字符串）
- 2空格缩进
- 行宽100字符
- 组件导出优先default export
- 条件渲染：简单用&&，复杂用三元或提取函数


---
description: NestJS后端代码规范 - 教师教研论文评选平台
globs:
  - "**/*.ts"
  - "**/*.js"
---

## 🎯 核心原则
- 遵循NestJS官方最佳实践和TypeScript严格模式
- 所有代码必须通过ESLint和Prettier检查后才能提交
- 使用依赖注入和模块化设计，避免紧耦合

## 📁 目录架构（严格遵守）
```
src/
├── modules/           # 业务模块（auth, users, competitions等）
│   └── [module]/
│       ├── entities/  # TypeORM实体
│       ├── dto/       # 数据传输对象
│       ├── *.controller.ts
│       ├── *.service.ts
│       └── *.module.ts
├── common/            # 通用组件
│   ├── decorators/    # 自定义装饰器
│   ├── guards/        # 守卫
│   ├── interceptors/  # 拦截器
│   ├── filters/       # 异常过滤器
│   ├── pipes/         # 管道
│   ├── dto/           # 通用DTO
│   ├── enums/         # 枚举
│   └── utils/         # 工具函数
└── config/            # 配置文件
```

## 🏷️ 命名约定
- **文件**: kebab-case（user-registration.service.ts）
- **类**: PascalCase（UserRegistrationService）
- **接口**: PascalCase + I前缀可选（IUserRepository）
- **枚举**: PascalCase（RegistrationStatus）
- **常量**: UPPER_SNAKE_CASE（MAX_FILE_SIZE）
- **变量/函数**: camelCase（getUserById）
- **私有属性**: 前缀_（private _cache）

## ✅ 必须遵循
1. **DTO验证**: 所有DTO使用class-validator装饰器
2. **Swagger文档**: Controller方法添加@ApiOperation和@ApiResponse
3. **异常处理**: 使用NestJS内置异常（BadRequestException等）
4. **路径别名**: 使用@/前缀（import { x } from '@/common/dto'）
5. **依赖注入**: 构造函数注入，禁止在类外部new实例
6. **事务处理**: 数据库写操作必须使用@Transaction装饰器
7. **日志记录**: 关键操作使用Logger记录（this.logger.log）

## 🚫 严禁行为
- ❌ 禁止any类型（除非有充分理由并添加注释）
- ❌ 禁止在Service层直接操作Request/Response对象
- ❌ 禁止硬编码敏感信息（密码、密钥等）
- ❌ 禁止在Entity中写业务逻辑
- ❌ 禁止循环依赖（使用forwardRef需说明原因）
- ❌ 禁止直接操作数据库（必须通过Repository）
- ❌ 禁止同步阻塞操作（使用async/await）

## 🔍 代码生成后强制检查
**每次生成或修改代码后，必须执行以下命令并确保通过：**
```bash
cd backend && npm run lint
```
如有错误，立即修复后再继续。不通过Lint检查的代码不得提交。

## 📝 代码模板
```typescript
// Controller示例
@Controller('users')
@ApiTags('Users')
export class UsersController {
  constructor(private readonly usersService: UsersService) {}

  @Get(':id')
  @ApiOperation({ summary: '获取用户详情' })
  async findOne(@Param('id') id: string) {
    return this.usersService.findOne(id);
  }
}

// Service示例
@Injectable()
export class UsersService {
  private readonly logger = new Logger(UsersService.name);

  constructor(
    @InjectRepository(User)
    private usersRepository: Repository<User>,
  ) {}

  async findOne(id: string): Promise<User> {
    const user = await this.usersRepository.findOne({ where: { id } });
    if (!user) throw new NotFoundException('用户不存在');
    return user;
  }
}
```

## 🎨 风格偏好
- 单引号，尾随逗号，2空格缩进
- 行宽100字符
- 箭头函数优先
- async/await优先于Promise.then

